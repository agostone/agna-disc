'use strict';

/**
 * Component types module
 *
 * @module agna-disc/component/Types
 */
module.exports = {
  value: require(__dirname + '/Value'),
  service: require(__dirname + '/Service'),
  factory: require(__dirname + '/Factory'),
  scope: require(__dirname + '/Scope')
};
